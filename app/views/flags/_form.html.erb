<%= form_for(flag) do |f| %>
  <% if flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flag.errors.count, "error") %> prohibited this flag from being saved:</h2>

      <ul>
      <% flag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, placeholder: "Name your flag", autofocus: true, required: true %>
  </div>

  <div class="field">
    <%= f.text_field :description, placeholder: "Tell us about the noises here...", required: true %>
  </div>

  <div class="actions">
    <%= f.submit "Plant it!"%>
  </div>
<% end %>
